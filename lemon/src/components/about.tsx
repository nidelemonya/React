import * as React from 'react';
// ts @types/react 
// 声明组件类型是 react 中没有状态的组件类型
export const About: React.StatelessComponent = () => {
    return (
        <div>
            About
        </div>
    )
}